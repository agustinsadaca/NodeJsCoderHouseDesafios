html 
  head 
    title Creación de productos
  body 
    each producto in productos
      div 
        h3 Nombre: #{producto.name}
        //h3=`Producto ${producto.name}`
        a(href=`/producto/${producto.id}`) Información 